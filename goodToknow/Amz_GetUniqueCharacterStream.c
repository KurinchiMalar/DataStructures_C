/* 
 * File:   Amz_GetUniqueCharacterStream.c
 * Author: kurnagar
 *
 * Created on June 9, 2014, 11:16 AM
 */

#include <stdio.h>
#include <stdlib.h>

/*
 * 
 */
/*char getUniqueCharacter() {
    HashMap<Character, Integer> hash = new HashMap<Character, Integer>();
    int pos = 0;
    while (hashNext()) {
        pos++;
        char c = getNext();
        if (hash.containsKey(c))
            hash.put(c, -1);
        else
            hash.put(c, pos);
    }
    char result = '#';
    pos++; // inexistent position
    for (Map.Entry<Character, Integer> entry : hash.entrySet()) {
         int p = entry.getValue();
         if (p != -1 && p < pos) {
              pos = p;
              result = entry.getKey();
         }
    }
    return result;
}*/
int mainTest(int argc, char** argv) {

    return (EXIT_SUCCESS);
}

